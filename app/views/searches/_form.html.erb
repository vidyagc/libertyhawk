<form action="search" accept-charset="UTF-8" method="get">
  <!--controller="searches"-->

  <div class="form-group">
    
    <table>
      <tr>
        <td><input type="text" name="subject" class="form-control" style="min-width:225px;" /></td>
        <td>

          <button type="submit" name="Search" value="Search" class="btn btn-basic" style="margin: 0 10px 0 10px;">
            <i class="fa fa-search" aria-hidden="true"></i>
          </button>
        </td>
      </tr>
    </table>
  
  </div>
</form>

<% if current_user && !current_user.bills.first.nil? %>
  <div class="panel panel-default" style="max-width: 350px">
    <div class="panel-heading">
      <div id="historyTog" class="historyToggle">Last 10 searches Hide</div>
      <div id="historyTogSm" class="historyToggle">Last 10 searches Show</div>
    </div>
    <div class="panel-body">
      <div id="history">
        <% current_user.searches.order('created_at DESC').each do |search| %>
    
          <table width="100%">
            <tr>
              <td align="left">
                <%= link_to controller: "searches", action: "search", method: "get", subject: search.metadata do %><u><%= search.metadata %></u><% end %>
              </td>
              <td align="right">
                <%= search.created_at.to_date %>
              </td>
            </tr>
          </table>

        <% end %>
      </div>
    </div>
  </div>
<% end %>

<!-- https://www.youtube.com/watch?v=FWIXWutlxIg 
putting dropdown in form 
could use form_tag vs form_for cuz it's not fields tied to attributes -->